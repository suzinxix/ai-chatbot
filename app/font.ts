import { Noto_Sans, Orbit } from "next/font/google";

export const notoSans = Noto_Sans({
  weight: "400",
  subsets: ["latin"],
});

export const orbit = Orbit({ weight: "400", subsets: ["latin"] });
